# Preprocessing Configuration for Label_Images Pipeline

input_images_path: "Dataset/images"
input_labels_path: "Dataset/labels"
output_path: "data/processed/v001/"
reports_path: "reports/"

classes:
  0: grape_bunch

split:
  train: 0.70
  validation: 0.15
  test: 0.15
  seed: 42

reproducibility:
  seed: 42
